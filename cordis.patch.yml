# dsh-rtk-optimizer — bundle layer (npm install form).
# Install with: dsh plugin --profile <name> add dsh-rtk-optimizer
# The row references the package by name so Node resolution finds the
# installed code. Source checkout users mount it via the profile's own
# cordis.patch.yml with `name: ./plugins/dsh-rtk-optimizer/index.js` instead.
- insert:
    - id: dsh-rtk-optimizer
      name: dsh-rtk-optimizer
